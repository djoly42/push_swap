
#include "push_swap.h"

void    init_a(t_env *env, char *av, int nb)
{
    int i;
    t_node = tmp;
    t_node =
    a_size = nb;
    i = 0;
    do
    {
     tmp = (t_node*)malloc(sizeof(t_node));
     tmp->prev = NULL;


}

int     main(int argc, char *argv)
{
    t_env   env;
    
    if (argc < 2)
        return (0);
    init_a(&env, argv, (argc - 1));


    return (0);
}
